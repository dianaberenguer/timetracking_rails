<h1><%= @project.name %></h1>
<ul>
 <% @entries.each do |entry| %>
   <li>
       <%= entry.hours %> h
       <%= entry.minutes %> m -
       <%= entry.date %>
   </li>
 <% end %>
</ul>
<%= link_to('Index', projects_path()) %>
<%= link_to('New entry', new_project_entry_path()) %>